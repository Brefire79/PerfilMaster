/**
 * config.js — STUB (Firebase removido, backend migrado para Supabase)
 *
 * Este arquivo existe apenas para compatibilidade caso alguma importação
 * referencie @/firebase/config. Nenhuma das funcionalidades do Firebase
 * está em uso — veja auth.js, firestore.js e functions.js para as
 * implementações Supabase correspondentes.
 */

export const app = null;
export const auth = null;
export const db = null;
export const functions = null;
export const storage = null;
export const analytics = null;
export default null;
